/*main {
    let a = vec2(-5.7, 2.1);
    print("Hello World !" as array);
}*/

//Benchmark fib

func fib(int n) int {
    if(n < 2) return n;
    return fib(n - 1) + fib(n - 2);
}

main {
    print(fib(30)); //34
}

/*
4348583 / 4173613 us
4sec
*//*
main {
    loop(1000000) {
        let i = 1 + 2.1 as int;
        float a = foo(i) * 2.0;
        string b = func() string { let a = "lol"; return a; }();
    }
}

func foo(int a) float {
	float b = -5.7;
	return b;
}*/

/*
Todo:
    - déclaration d'une variable locale et d'une fonction du même nom

    Suggestions:
    Système de channel comme mosaiq
    Variables globales
    Type Object (orienté prototype)
    Type Pointeur (type uniquement accessible dans les primitives)
    Multiples valeurs de retour: a, b = foo(); func() int, int {}
*/